apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "launchly-project.fullname" . }}-test-connection"
  labels:
    {{- include "launchly-project.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "launchly-project.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
